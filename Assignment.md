# Taskly - Приложение для управления задачами

## Технологический стек

### Фронтенд
- **Vue.js 3** 
- **TypeScript**
- **Tailwind CSS** 
- **Vite** 
- **Pinia** 
- **Vue Router** 

### Бэкенд
- **Express.js** 
- **TypeScript** 
- **TypeORM**
- **PostgreSQL**
- **JWT** 

## 📁 Структура проекта

### Фронтенд (`todoList/src`)
```
src/
├── api/              # Слой интеграции с API
├── assets/           # Статические ресурсы (изображения, шрифты)
├── components/       # Переиспользуемые Vue компоненты
├── composables/      # Vue composables (общая логика)
├── locales/          # Переводы i18n
├── router/           # Конфигурация Vue Router
├── stores/           # Управление состоянием Pinia
├── styles/           # Глобальные стили
├── utils/            # Утилиты и помощники
├── views/            # Компоненты страниц
├── App.vue           # Корневой компонент
├── main.ts           # Точка входа приложения
```

### Бэкенд (`todoListBackend/src`)
```
src/
├── config/           # Файлы конфигурации
├── controllers/      # Обработчики маршрутов и бизнес-логика
├── dtos/             # Data Transfer Objects
├── entities/         # Модели сущностей TypeORM
├── middlewares/      # Express middleware (аутентификация, валидация)
├── repositories/     # Слой доступа к базе данных
├── routes/           # Определение API маршрутов
├── scripts/          # Вспомогательные скрипты
├── services/         # Сервисы бизнес-логики
├── types/            # TypeScript типы
├── utils/            # Вспомогательные функции
├── app.ts            # Инициализация Express приложения
├── main.ts           # Точка входа сервера
```



### Установка бэкенда

1. **Перейдите в директорию бэкенда:**
   ```bash
   cd todoListBackend
   ```

2. **Установите зависимости:**
   ```bash
   npm install
   ```

3. **Настройте переменные окружения:**
   Создайте файл `.env` в директории `todoListBackend`:
   ```
    PORT=3000

    DB_HOST=localhost
    DB_PORT=5432
    DB_USER=postgres
    DB_PASSWORD=ваш пароль
    DB_NAME=taskly_db

    JWT_SECRET=ваш ключ
    JWT_EXPIRE=7d

  	CORS_ORIGIN=http://localhost:5173
   ```


5. **Создайте БД taskly_db:**
   

6. **Запустите сервер бэкенда:**
   ```bash
   npm run dev
   ```
7. **Запустите скрипт, он создаст юзера, проекты и задачи**
   ```bash
   npm run seed
   ```
   

   
   Данные пользователя: l - teamLead; p - 123123

### Установка фронтенда

1. **Перейдите в директорию фронтенда:**
   ```bash
   cd todoList
   ```

2. **Установите зависимости:**
   ```bash
   npm install
   ```

3. **Запустите сервер разработки:**
   ```bash
   npm run dev
   ```

   Приложение будет доступно по адресу `http://localhost:5173`
   
   
  ### Дополнительные пункты
  
  ```
- Аутентификация
- Категории заметок
- Полнотекстовый поиск
- Обработка ошибок 
- Валидация
```


